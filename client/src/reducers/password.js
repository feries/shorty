const initialState = {
  isReset: true,
  oldPassword: '',
  newPassword: '',
  rePassword: '',
  passwordValid: true
}

export default (state = initialState, action) => {
  switch (action.type) {
    default:
      return state
  }
}
